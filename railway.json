{
  "deploy": {
    "startCommand": "cd backend && python3 -m pip install -r requirements.txt && python3 app.py ${PORT:-5799}",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
